# CrewHQ hotspot size bump

Unzip and merge into your repo root — folders match your project structure.

## Change from the last zip
Only `CrewHQ.jsx` changed — the three table hotspots (badge, icon, label)
are bigger and the label text is now bold:
- badge 46px -> 58px
- icon 20px -> 26px
- label 7px -> 10px, bold

Tests (352/352) and the production build both pass.
